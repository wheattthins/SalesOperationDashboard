# Neon (production / hosted). Run `npx neonctl env pull` after linking a project in .neon
DATABASE_URL="postgresql://..."
DATABASE_URL_UNPOOLED="postgresql://..."

# Local SQLite alternative — change prisma/schema.prisma provider to "sqlite" and use:
# DATABASE_URL="file:./dev.db"
